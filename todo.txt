  subcmd: `zap setup-context` for a new context
  subcmd: `zap context-exclude`
  subcmd: `zap context-include`
  subcmd: `zap context-include`
  subcmd: `workflows find-params` to find all unique params per endpoint
